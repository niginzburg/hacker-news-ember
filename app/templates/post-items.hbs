{{page-title "PostItems"}}
<Navbar 
    @requestedId='posts-tab'
    @controller={{this}} 
    @viewingRoute="Posts" />
<NavbarPosts
   @controller={{this}} />

<PostItems
    @model={{@model}}
    @controller = {{this}}/>

<Footer />